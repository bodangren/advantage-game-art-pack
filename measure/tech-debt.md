# Tech Debt Registry

> This file is curated working memory, not an append-only log. Keep it at or
> below 50 lines.

| Date | Track | Item | Severity | Status | Notes |
|------|-------|------|----------|--------|-------|
| 2026-04-05 | sprite_compiler_mvp_20260405 | Initial renderer uses procedural shapes instead of a reusable external part library. | Medium | Open | Acceptable for MVP because it proves deterministic sheet export and validation first. |
| 2026-04-24 | renderer_palette_refinement_20260424 | The candidate loop exists, but the current renderer still trips the palette-limit structural check and usually regenerates instead of selecting a survivor. | High | Closed | Fixed in commit 439d77b — median-cut palette quantization wired into all three compiler paths (character_sheet, prop_or_fx_sheet, tileset); renderer output now respects style_pack.palette_limits. |
| 2026-04-24 | llm_planner_provider_impl_20260424 | Planner provider interface is defined but no concrete implementation exists yet (OpenAI, Anthropic, etc.). | Medium | Closed | Implemented OpenAIProvider and AnthropicProvider with urllib-based HTTP calls, exponential backoff retry, and graceful fallback when no API key is configured. |
| 2026-04-24 | batch_generation_release_manifests_20260405 | Batch runner schema and orchestrator implemented; actual planner/compiler integration pending provider implementation. | Medium | Closed | State machine, persistence, and retry policies working. Real LLM calls now possible with the provider implementations. |
| 2026-04-24 | batch_generation_release_manifests_20260405 | Release bundle exporter stub copies candidate dir contents; actual rendered PNG files need real compiler output. | Medium | Open | Bundle structure and audit reports are correct. Actual asset copy needs compiled PNGs from the candidate loop. |
| 2026-04-05 | primitive_library_promotion_pipeline_20260405 | Seed library still lacks finer-grained primitives such as robe hems, hats, shelf modules, tile edges, rune decals, and contact-shadow slices. | Medium | Closed | Fixed 2026-05-02 — Added scene-specific tiles (floor_stone_01, wall_library_01, cobble_01) and props (bookshelf_01, ruins_pillar_01, rubble_01) for library_room and ruins_courtyard scene templates. |
| 2026-04-09 | scene_layout_background_assembler_20260405 | Scene assembler schema is defined but no approved tiles or props exist for library_room/ruins_courtyard templates yet. | Medium | Closed | Fixed 2026-05-02 — Added 6 scene primitives (floor_stone_01, wall_library_01, cobble_01, bookshelf_01, ruins_pillar_01, rubble_01) with source.png files and metadata. Scene programs now compile and assemble correctly. |
| 2026-04-11 | scene_layout_background_assembler_20260405 | Layout resolver had a boolean guard for prop intrusions but never checked the flag before appending placements, allowing intrusions silently. | High | Closed | Fixed in commit 7eb30a5 — resolver now raises LayoutResolutionError immediately when any prop overlaps a reserved zone. |
| 2026-04-13 | scene_layout_background_assembler_20260405 | `resolve_scene_layout` assigns every prop `bounds=(0,0,32,32)`; both branches of the `tile_source` check are identical dead code. All props pile at origin and multi-prop scenes would spuriously intrude. | High | Closed | Fixed in cb8dd1b — resolver now reads tile source.png size and uses seeded hash-based placement for distinct non-overlapping bounds. Reserved zones are checked during placement loop. |
| 2026-04-13 | scene_layout_background_assembler_202embler_20260405 | `_apply_lighting_pass` runs pure-Python per-pixel loops over the full canvas twice per render; diagonal directions double-darken the overlapping quadrant. | Medium | Closed | Fixed in a94c307 — replaced double-apply with single combined ambient+directional factor; shadow direction corrected for all 8 cardinal/diagonal directions. |
| 2026-04-23 | lighting_renderer_fixes_20260423 | Visual regression fixtures require actual primitive PNGs which don't exist (system uses procedural sprites). Phase 3 (pixel-diff) skipped; unit tests provide correctness verification. | Low | Closed | Fixed 2026-05-02 — Added source.png files for all scene primitives. Smoke tests now compile and assemble scene programs successfully. |
| 2026-04-13 | scene_layout_background_assembler_20260405 | `_resolve_tile_image` returns None when only `primitive.json` exists (no source.png), silently dropping the tile from the render. | Low | Open | Log a warning or raise; silent visual omission is hard to debug downstream. |
| 2026-04-14 | presentation_surfaces_ui_pipeline_20260405 | Cover and loading surface background resolution uses a simplified convention: outputs/{scene_program_name}/base.png. Actual scene rendering may place the output elsewhere. | Medium | Open | Align with the scene assembler output convention; prefer passing an explicit resolved image path rather than deriving it from the manifest name. |
| 2026-04-14 | presentation_surfaces_ui_pipeline_20260405 | Parallax tiling uses a pseudo-random offset `(x_offset * 3) % canvas_w` which is deterministic but not analytically reproducible for repeat-safe edge handling. | Low | Open | Use a seeded deterministic grid or tile-flipping instead of pseudo-random offsets for guaranteed edge seamlessness. |
| 2026-04-14 | presentation_surfaces_ui_pipeline_20260405 | UI sheet layout is a naive left-to-right row wrap; no bin-packing or atlas efficiency optimization. Acceptable for MVP, but icon-dense sheets will waste canvas space. | Low | Open | Consider a best-fit bin-packing layout if canvas utilization becomes a concern for dense UI packs. |
| 2026-04-14 | presentation_surfaces_ui_pipeline_20260405 | Promo capture job loads a pre-rendered image from `source_bundle/promo.png`; actual gameplay capture would require a running game or scene-render replay path that is not yet implemented. | High | Closed | Fixed in promo_capture_scene_renderer_20260425 — assemble_promo_capture_job now checks capture_conditions.scene_program and renders the scene via assemble_scene when specified; falls back to static promo.png when scene_program is empty. |
| 2026-04-16 | review_queue_foundation_20260405 | Review app serves server-rendered HTML but has no image file serving; preview placeholders show "Preview" text instead of actual images. | Medium | Closed | Fixed in review_app_serving_20260425 — StaticFiles mounted at /static for outputs directory; templates now render actual images. |
| 2026-04-24 | review_app_auth_and_images_20260424 | No authentication on review app; any user can approve/reject candidates. | High | Closed | Added API key auth middleware with X-Api-Key header and query param support. |
| 2026-04-24 | review_app_auth_and_images_20260424 | Review app serves server-rendered HTML but has no image file serving; preview placeholders show "Preview" text instead of actual images. | Medium | Closed | Added StaticFiles mount at /static for outputs directory; templates now render actual images from rendered_files paths. |
| 2026-04-17 | prompt_to_asset_program_planner_20260405 | Planner provider interface is defined but no concrete implementation exists yet (OpenAI, Anthropic, etc.). | Medium | Open | Provider abstraction allows swapping implementations; actual API calls need credentials and implementation. |
| 2026-04-24 | critic_stack_auto_approval_20260405 | Family adapters still use mocked style baseline when no demo references exist; real calibration requires actual scene/surface demo assets. | Medium | Open | Threshold packs exist and tests are passing; end-to-end calibration needs real reference images for background_scene, cover_surface, etc. |
| 2026-04-24 | critic_stack_auto_approval_20260405 | Policy aggregator is in-memory; a command-line recalibration CLI (`recalibrate --family X`) does not yet exist. | Low | Closed | Implemented as `asf candidate recalibrate --family X [--style-threshold N] [--novelty-threshold N] [--target-pass-band lower,upper]`. Calibrate command also gains --family filter. |
| 2026-04-25 | orchestrator_consolidation_20260426 | BatchRunner and BatchOrchestrator duplicate overlapping batch state machine logic (planning → compiling → candidate_loop → critic_scoring → review_routing). | Medium | Closed | Consolidated into single BatchOrchestrator in batch_orchestrator.py; batch_runner.py now delegates to BatchOrchestrator with deprecation warning. |
| 2026-04-25 | batch_generation_release_manifests_20260405 | Release bundle exporter stub copies candidate dir contents; actual rendered PNG files need real compiler output. | Medium | Closed | BatchOrchestrator now calls compile_program for each asset in _run_compiling, and candidate loop via build_candidate_job + run_candidate_job in _run_candidate_loop. Assets are properly compiled and candidate loop runs to produce actual PNGs. |
| 2026-04-25 | review_app_auth_and_images_20260424 | `_utc_now()` utility is duplicated across batch_orchestrator.py, batch_runner.py, and batch_exporter.py. | Low | Open | Extract to a shared utility module (e.g., asf.utils) to avoid repetition. |
| 2026-05-02 | orchestrator_consolidation_20260426 | `_generate_programs` and `_write_programs` methods in BatchOrchestrator are defined but never called. | Low | Open | Either wire these into the planning stage or remove them if planner integration is handled elsewhere. |
| 2026-05-02 | review_app_serving_20260425 | FastAPI authentication via Depends(require_auth) applies to all routes including POST actions. | Medium | Open | Use BaseHTTPMiddleware for path-level exemptions if needed. |
| 2026-05-02 | directional_character_sheets_20260502 | DirectionalSheetProgram has no example program files or CLI subcommand yet. | Low | Closed | Example programs added at programs/directional_sheet/ (knight_walk_4dir.json, mage_idle_8dir.json). CLI works via generic `asf compile --program`. |
| 2026-05-02 | directional_character_sheets_20260502 | Directional frames are identical across directions (no actual directional variation). | Medium | Closed | Style pack now supports directional_variants dict; _apply_variant_adjustments applies direction-aware offset per direction (N/S/E/W ±2px). |
| 2026-05-03 | effect_overlay_pipeline_20260502 | Effect sheet compiler uses dummy PaletteSpec for style pack loading; actual effect rendering does not use palette quantization. | Low | Open | Consider passing real palette hints to effect compilers if palette quantization is desired for effect sheets. |
