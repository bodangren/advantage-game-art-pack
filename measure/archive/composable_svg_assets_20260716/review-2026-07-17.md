File writes are blocked in this session, so I couldn't save the report into the empty `review-2026-07-17.md` placeholder — here it is as output instead. All quality gates were re-verified live today: typecheck clean, 79/79 tests passing, production build succeeds.

# Review Report: Composable SVG Asset Factory Pivot (`composable_svg_assets_20260716`)

## 1. Summary of Changes

The track landed as a single pivot commit, `68dfd51` (2026-07-16 12:20), with **+4,045 / −31,499 lines**:

- **Removed** the entire retired Python/raster factory: `src/asf/`, `src/review_app/`, `tests/`, `canon/`, `library/`, `programs/`, `critic_thresholds/`, `reference/`, `pyproject.toml`.
- **Engine**: `src/lib/svg-assets.ts` (796 lines) — strict part-metadata validation, safe-SVG-subset validation, composition-spec normalization, anchor/direct placement via matrix transforms, deterministic serialization, SHA-256 asset metadata, and a Phaser load config.
- **Catalog & parts**: `src/lib/catalog.ts` plus 4 checked-in parts (body, shirt, hair, sword) with JSON metadata; `default-spec.ts`; `examples/svg_character.json`.
- **Desk**: `pages/index.tsx` (244 lines at commit time) with live composition controls, SVG export download, and Phaser-config copy.
- **Docs**: rewrote `README.md`, `DESIGN.md`, and the measure docs (`product.md`, `tech-stack.md`, `workflow.md`, `product-guidelines.md`); retired Python-era tracks in the registry.

The remaining commits in the 24-hour window (`3d2d95c`…`72d7dde`) belong to three follow-on tracks (animation timelines, directional sheets, per-game bundles). Notably, they built on this track's engine **without modifying it** — reusing `sha256`, `composeSvg`, and the catalog — good evidence the contract held.

## 2. Spec Alignment

| Acceptance criterion | Status | Evidence |
|---|---|---|
| Strict validation contracts for metadata + SVG source | **Met** | `validateSvgPartMetadata` exact-key checks; `validateSvgSource` element/attribute allowlists, script/external-ref bans |
| Catalog as JSON for LLM prompt context | **Met** (nuance) | Metadata lives as checked-in `part.json`, validated at import; no dedicated "dump catalog" helper, though `SVG_PARTS[*].metadata` is directly serializable |
| Strict composition spec (viewBox, optional output, palette, direct + anchor placement) | **Met** | `normalizeCompositionSpec` with defaults, exact keys, bounds checks |
| Deterministic SVG, stable layer order, palette CSS vars | **Met** | z-sort with source-index tiebreak; sorted `:root` variables; determinism tested |
| Metadata: source spec, resolved parts, viewBox, output, SHA-256 | **Met** | `composeSvgAsset` → `SvgAssetMetadata` |
| Desk demonstrates variable sizing + anchor attachment | **Met** | 64-unit viewBox rendered at 256×256; anchor-attached shirt/hair/sword; export + copy actions |
| Typecheck, tests, build pass | **Met — re-verified today** | `tsc --noEmit` clean, vitest 79/79, `vinext build` completes |

All plan phases 1–6 are checked off and match the committed work; out-of-scope boundaries (no raster export, no LLM integration, no PNG imports) were respected.

## 3. Code Quality Observations

- **Strong validation discipline**: exact-key object validation, magnitude caps, cycle detection in anchor attachment, non-finite transform guards, contextual error messages.
- **Thin direct test coverage** (Medium): 5 tests / 103 lines against a 796-line engine. Untested negative paths: cyclic `attach_to`, missing palette values, duplicate placement/part IDs, z-index overrides, rotation/scale math, `buildPhaserLoadConfig` limits. Follow-on tracks exercise happy paths indirectly only.
- **Regex-based SVG validation** (Medium, hardening): the attribute scan requires quoted values, so an unquoted attribute (e.g. `<rect style=fill:red>`) evades the allowlist. Residual risk is low — parts are checked-in, not user-supplied, and `url(`/`javascript:`/`https?://` are banned globally — but worth closing.
- **Minor**: `composeSvgAsset` runs `resolvePlacements` twice (once inside `composeSvg`, once for metadata); the injected style id `asf-palette` is naming residue from the retired Python project.
- **Known friction, already captured in lessons-learned**: composed output contains `<style>`, which `validateSvgSource` bans, so compositions can't be re-validated as parts — this surfaced downstream in the atlas track.

## 4. Risks or Blockers

- **No blockers.** The track is complete, and three downstream tracks shipped on top of the engine unchanged.
- `pages/index.tsx` has since grown to 465 lines across three docks — an emerging junk-drawer risk for the desk page.
- `sha256` requires Web Crypto (`crypto.subtle`), unavailable in non-secure contexts; guarded with a clear error, but relevant to any future non-localhost deployment.
- Registry hygiene: `measure/tracks.md` has two `## Upcoming Tracks` headings, and this is the only completed track still under `measure/tracks/` instead of `measure/archive/`.

## 5. Recommended Next Actions

1. **Backfill negative-path unit tests** for `svg-assets.ts` (cycles, missing palette, duplicates, z-overrides, dimension caps).
2. **Harden `validateSvgSource`** to reject unquoted attributes, closing the allowlist bypass.
3. **Archive the track** to `measure/archive/`, update its registry link, and dedupe the "Upcoming Tracks" heading.
4. **Extract the desk docks** in `pages/index.tsx` into components before the next desk-facing track.
5. Cosmetic: rename the `asf-palette` style id (e.g. `svg-palette`).

If you re-run me with write permission, I can drop this report into `measure/tracks/composable_svg_assets_20260716/review-2026-07-17.md` (currently an empty placeholder) and optionally handle the archive step.
