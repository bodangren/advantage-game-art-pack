# Review: `mockup_art_pass_20260717` (archived) — last 24h

**Verdict: the track fully met its spec.** All 13 plan tasks are done, all five acceptance criteria are satisfied, and I independently verified the suite is green (114/114 tests, plus typecheck) at the track's final commit `38bcc62` using a throwaway worktree. The 3 test failures in the *current* working tree come from the uncommitted follow-on track, not this one.

## 1. Summary of changes

Four commits belong to the track:

- **`07e723e`** (Phases 1–2): 22 curated mmx reference mockups (5 archetypes, 4 props, 2 FX) checked into `demo-assets/reference/` with a `manifest.json` mapping each file to archetype, prompt, candidates, and target part IDs; new `ramp-convention.test.ts` pinning the Red contract.
- **`2e6c27c`** (Phases 3–4): all 26 library parts reworked with 3-tone ramps (`<slot>`/`-light`/`-shadow`), top-left lighting, banded shadows, and sparse dither; a new `fx/shadow-ground` part placed under every character; DESIGN.md/README shading vocabulary docs; all example palettes and fixtures re-frozen; lessons-learned updated. 76 files, +1440/−316.
- **`76ac7ce` / `38bcc62`**: completion (13/13 tasks, estimation exact) and archival.

A fifth commit in the window, `c55854e` (knight plate armor, cape, helmet, longsword + `measure/review.html`/`review-scratch.mts`), belongs to the **next** track (`svg_art_quality_iteration_20260717`), not this one.

## 2. Spec alignment

| Acceptance criterion | Status |
|---|---|
| Reference mockups + manifest for all archetypes | **Met** — 22 jpgs, manifest covers all 5 archetypes plus props/FX with prompts and per-part targets |
| Palettes carry light/base/shadow ramps, parts use them | **Met** — verified in part metadata (e.g. `body-goblin` declares `skin`/`skin-light`/`skin-shadow`) and enforced by `ramp-convention.test.ts` |
| Shading shapes present (highlights, form shadows, ground shadow) | **Met** — part SVGs contain highlight/shadow bands; `shadow-ground` placement below the body is test-enforced for every character |
| Each archetype visibly tracks its mockup in browser review | **Met (per commit evidence)** — commit states browser-verified; I can't independently re-verify the visuals, but the render-API loop and review tooling exist |
| Fixtures re-frozen; typecheck/test/build pass | **Met** — verified green at `38bcc62` |

Non-functional requirements held too: no dialect changes (no blur/filters/gradients appear in the reworked SVGs), exact-key metadata, deterministic re-frozen digests. One bookkeeping nit: the acceptance checkboxes in the archived `spec.md` were never ticked, even though the plan is fully `[x]`.

## 3. Code quality observations

- `ramp-convention.test.ts` is well built: it loads fixtures via `fs` so the suite stays importable during the Red phase (matching the lesson recorded from the previous track), and its assertions carry per-asset failure messages.
- The manifest is genuinely useful metadata, not a stub — it records the shared style suffix, light-direction notes, and which candidate mockup was selected per archetype.
- The lessons-learned entry added is a real, reusable insight (palette-slot additions have library-wide blast radius; grep for positional `parts[N]` pins). Note it displaced an older "Patterns That Worked Well" entry to stay within the file's line bound.
- Minor: the shadow test's z-index fallbacks (`shadow.z_index ?? 1`, `body.z_index ?? 10`) would silently pass if both fields were omitted, and ramp *usage* is only asserted for body parts — acceptable given the spec wording, but shirts/hair rely on the broader palette-slot sync test instead.
- Minor: ~2 MB of reference JPEGs are now in git history. Fine at this scale; worth watching if mockup curation becomes a per-track habit.

## 4. Risks or blockers

- **The working tree is currently red.** 36 files of uncommitted edits from the follow-on art-quality track cause 3 failures: stale composition digests, a part whose `palette_slots` metadata declares `cloth-light`/`cloth-shadow` that its edited SVG no longer references, and a timeout in the knight determinism test. None of this blocks the archived track, but it means the follow-on track is mid-flight with unfrozen fixtures.
- The knight determinism timeout is exactly the gotcha already recorded in lessons-learned (child/heavy tests outgrow the 5s default) — `knight-example.test.ts` never got an explicit `testTimeout`, so it will keep flaking as compositions grow.
- Browser-visual acceptance rests on commit-message attestation; there's no persisted screenshot artifact tying each archetype to its mockup (the new track's `measure/review.html` looks like it's starting to address this).

## 5. Recommended next actions

1. In the active track, re-sync `palette_slots` metadata with the edited SVGs and re-freeze composition digests before the next commit — the failing tests point at exactly which parts drifted.
2. Add an explicit `testTimeout` to the knight determinism test now; it's a two-line fix for a known recurring flake.
3. Tick the acceptance-criteria checkboxes in archived specs at completion time (small process fix for the complete/archive step).
4. Consider capturing the browser-review output (e.g., a rendered comparison sheet per archetype) as a committed artifact so "visibly tracks its mockup" is auditable after archival.
